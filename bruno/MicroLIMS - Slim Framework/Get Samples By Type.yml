info:
  name: Get Samples By Type
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/api/samples?type=efluente
  params:
    - name: type
      value: efluente
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
