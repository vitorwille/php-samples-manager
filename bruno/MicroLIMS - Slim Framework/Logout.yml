info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/logout
  body:
    type: json
    data: |-
      {
      "email": "vitssdor@gmail.com",
      "password": "12345c678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
