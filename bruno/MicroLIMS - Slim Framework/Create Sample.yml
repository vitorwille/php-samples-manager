info:
  name: Create Sample
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/samples
  body:
    type: json
    data: |-
      {
      "sampleType": "efluente",
      "sampleTechnician": "Vitor",
      "sampleReceivalDate": "2026-08-14"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
