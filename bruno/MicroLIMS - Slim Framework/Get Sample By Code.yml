info:
  name: Get Sample By Code
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/samples?code=amostra1
  params:
    - name: code
      value: amostra1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
