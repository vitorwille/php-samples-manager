info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
      "name": "teste",
      "email": "vitssddodr@gmail.com",
      "password": "12345c678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
