info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/login
  body:
    type: json
    data: |-
      {
      "email": "vitssdor@gmail.com",
      "password": "12345c678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
