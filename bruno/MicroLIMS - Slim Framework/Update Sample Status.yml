info:
  name: Update Sample Status
  type: http
  seq: 9

http:
  method: PATCH
  url: http://localhost:8080/api/samples
  body:
    type: json
    data: |-
      {
        "sampleCode": "amostra06",
        "sampleStatus": "em_analise"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
