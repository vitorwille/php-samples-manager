info:
  name: Search Samples By Code
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/samples?search=am
  params:
    - name: search
      value: am
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
