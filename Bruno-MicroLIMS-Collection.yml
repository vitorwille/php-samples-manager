opencollection: 1.0.0
info:
  name: MicroLIMS - Slim Framework
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ''
      port: ''
      auth:
        username: ''
        password: ''
      bypassProxy: ''
items:
  - info:
      name: Get Users
      type: http
      seq: 1
    http:
      method: GET
      url: http://localhost:8080/api/users
      auth: inherit
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
  - info:
      name: Create User
      type: http
      seq: 2
    http:
      method: POST
      url: http://localhost:8080/api/users
      body:
        type: json
        data: |-
          {
          "name": "teste",
          "email": "vitssddodr@gmail.com",
          "password": "12345c678"
          }
      auth: inherit
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
  - info:
      name: Login
      type: http
      seq: 3
    http:
      method: POST
      url: http://localhost:8080/api/login
      body:
        type: json
        data: |-
          {
          "email": "vitssdor@gmail.com",
          "password": "12345c678"
          }
      auth: inherit
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
  - info:
      name: Logout
      type: http
      seq: 4
    http:
      method: POST
      url: http://localhost:8080/api/logout
      body:
        type: json
        data: |-
          {
          "email": "vitssdor@gmail.com",
          "password": "12345c678"
          }
      auth: inherit
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
bundled: true
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    exportedAt: '2026-08-14T17:31:17.244Z'
    exportedUsing: Bruno
